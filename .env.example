# Optional: Server-side fallback API key (if you want the app to work without user-provided keys)
# Most users will enter their own key via the Settings panel in the browser.
# ANTHROPIC_API_KEY=sk-ant-xxxxx
